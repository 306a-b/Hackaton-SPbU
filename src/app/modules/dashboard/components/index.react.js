import React, { PropTypes }         from 'react';

class Container extends React.Component {
    static propTypes = {

    };

    constructor() {
        super();
    }

    render() {
        return (
            <div>
                Dashboard
            </div>
        );
    }
};

export default Container;
